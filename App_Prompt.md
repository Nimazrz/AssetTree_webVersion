# پرامپت توصیف کامل اپلیکیشن مدیریت پورتفو و دارایی‌ها

از متن زیر می‌توانید به عنوان یک پرامپت جامع برای توصیف این اپلیکیشن به هوش مصنوعی یا برنامه‌نویسان استفاده کنید:

***

**متن پرامپت:**

«من یک اپلیکیشن اندروید می‌خوام که با Kotlin و Jetpack Compose نوشته شده باشه. این اپلیکیشن یک ابزار پیشرفته برای مدیریت، تحلیل و نمایش سلسله‌مراتبی (درختی) دارایی‌ها و پورتفوی مالی است. لطفاً اپلیکیشن را با رعایت دقیق مشخصات زیر بساز:

### ۱. معماری و ساختار داده:
- برنامه باید دارای یک موتور پردازش درختی (TreeEngine) باشد تا بتواند داده‌های مالی (نماد، تعداد سهم، قیمت، ارزش ریالی، نام شرکت، نوع دارایی) را مدیریت کند.
- امکان محاسبه خودکار ارزش گره‌های والد بر اساس مجموع ارزش گره‌های فرزند (محاسبه Bottom-Up).
- پشتیبانی از ورود داده‌ها (Import) از طریق کپی/پیست از فایل‌های اکسل. ستون‌های خواندن داده‌ها: نام دارایی (ستون ۲)، تعداد سهم (ستون ۴) و ارزش ریالی (ستون ۶) تنظیم شوند.

### ۲. امکانات و عملکردهای اصلی (Features):
- **مدیریت گره‌ها:** قابلیت افزودن دارایی جدید، ویرایش مقادیر، حذف، و انتقال (Move) یک دارایی به یک دسته‌بندی دیگر.
- **کتابچه نمادها (Symbol Book):** بخشی اختصاصی برای مدیریت نام نمادها و تعیین ضرایب خاص برای هر کدام.
- **تاریخچه عملیات (Undo/Redo):** سیستمی یکپارچه برای ذخیره تغییرات و امکان بازگردانی (Undo) آن‌ها در صورت اشتباه.
- **پنل تنظیمات:** منوی تنظیمات برای تغییر قالب‌ها، تغییر حالت نمایش پیش‌فرض، و بازنشانی (Reset) داده‌ها.

### ۳. طراحی ظاهری (UI/UX) و تم‌ها:
- رابط کاربری باید بسیار مدرن، روان و بر اساس استانداردهای **Material Design 3** باشد.
- استفاده از گوشه‌های کاملاً نرم و گرد (شعاع‌های ۱۶ تا ۲۴ دی‌پی) برای کارت‌ها، دیالوگ‌ها و منوها (Modern Shapes).
- برنامه باید دارای نوار ابزار بالا (Top Bar) با دکمه‌های کپسولی (Pill-shaped) برای جابجایی بین نماهای مختلف باشد. کارت هدر اصلی باید ارزش کل پورتفو را با افکت‌های شفافیت ملایم (Floating effect) نشان دهد.
- پشتیبانی از تم‌های مختلف روز و شب از جمله تم‌های تاریک ویژه (AMOLED Night و Navy Classic) با پالت رنگی حرفه‌ای.

### ۴. نماها و صفحات مختلف برنامه (Views):
برنامه باید دارای ۶ صفحه/نمای اصلی برای تحلیل داده‌ها باشد که کاربر بتواند به نرمی بین آن‌ها جابجا شود:
- **درخت کلاسیک (Classic Tree):** نمایش خطی و بازشونده دسته‌بندی‌ها به صورت لیست استاندارد.
- **درخت مدرن (Modern Tree):** نمایش درختی با کارت‌های بصری زیبا، نشانگرهای رنگی در کنار هر کارت و سایه‌های نرم.
- **نقشه درختی (Treemap Chart):** نمایش ارزش دارایی‌ها به صورت مستطیل‌های تودرتو متناسب با وزن و ارزش هر دارایی در پورتفو.
- **نمودار خورشیدی (Sunburst Chart):** نمایش شعاعی و حلقوی (پای‌چارت چندلایه و تعاملی) برای درک بهتر نسبت دسته‌بندی‌ها به کل.
- **نمودار میله‌ای (Bar Chart):** برای مقایسه مستقیم ارزش دارایی‌های هم‌سطح در کنار هم.
- **داشبورد تحلیلی (Analytics Dashboard):** صفحه‌ای شامل گزارش‌های آماری (تعداد کل گره‌ها، عمیق‌ترین سطح)، بررسی صحت محاسبات، و ارائه گزارش‌های شفاف در صورت عدم تطابق محاسبات پایین‌به‌بالا با مقادیر ثبت‌شده.

لطفاً کدها را با ساختار ماژولار و تفکیک‌شده (پوشه‌های UI, Data, Utils, Theme) پیاده‌سازی کن و از کامپوننت‌های انیمیشن‌دار Compose برای تغییرات صفحات استفاده نما.»
